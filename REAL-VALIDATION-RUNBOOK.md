# دليل التحقق الواقعي

هذا الملف يميز بين الاختبارات المحلية الناجحة وبين التحقق الخارجي الذي يحتاج بيئة لا يوفرها المتصفح الحالي.

## متطلبات التشغيل

يلزم مورد Azure Document Intelligence بمفتاح صحيح، وملفات benchmark عربية ومختلطة حقيقية مع النص المرجعي والجداول المرجعية، وWord وExcel مكتبيان أو حساب Microsoft 365 قادران على sideload لإضافة `manifest.xml`. لا يجوز استخدام قيم عشوائية أو ملفات اصطناعية عند قياس الدقة.

## تسلسل التشغيل

1. ضبط `AZURE_DOCUMENT_INTELLIGENCE_ENDPOINT` و`AZURE_DOCUMENT_INTELLIGENCE_KEY` عبر مدير الأسرار، ثم اختيار مزود Azure.
2. تشغيل PDF النصي، PDF الممسوح، PDF المختلط، PNG/JPG، والنصوص العربية المختلطة بالإنجليزية والفرنسية.
3. تسجيل Character Accuracy وWord Accuracy، وعدد الصفوف والأعمدة والخلايا الصحيحة، وحالات الدمج، والزمن لكل ملف.
4. تحميل الإضافة عبر sideload في Word واختبار PDF→Word والصورة→Word والتحويل الذكي، ثم تكرار ذلك في Excel لكل أوضاع توزيع الأوراق.
5. حفظ سجل النتيجة مع إصدار المشروع، نوع المزود، اسم الملف غير الحساس، الزمن، التحذيرات، ونتيجة الإدراج. لا تُحفظ الملفات الأصلية أو المفاتيح داخل المستودع.

## ما تم قياسه محليًا

تم قياس عقود التحويل، RTL/LTR، ترتيب الأعمدة، preprocessing للصور، التحقق المحافظ من PDF، الدمج والجداول، i18n، ورسائل الأخطاء محليًا. آخر نتيجة محلية: 28 اختبارًا ناجحًا. هذه الأرقام لا تمثل دقة OCR إنتاجية ولا نجاح sideload داخل Office.
