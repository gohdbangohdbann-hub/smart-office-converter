# تقرير المرحلة الثالثة — Nawa OCR Office / Excel

## الحالة التنفيذية

تم تنفيذ طبقة Excel الأولية داخل Task Pane مع إبقاء النطاق محصورًا في المرحلة الثالثة وعدم بدء المرحلة الرابعة. تعتمد الطبقة على نموذج مستقل عن Excel.js، ثم ينفذ adapter الإدراج داخل `Excel.run` باستخدام `Worksheet`, `Range.values`, و`Table` عندما لا يوجد دمج متعارض. هذا يتوافق مع توثيق Microsoft الذي يعرّف الخلايا كنطاقات Range ويستخدم مصفوفات ثنائية الأبعاد للقيم، كما يوضح إنشاء وإدارة الجداول عبر Excel JavaScript API [1] [2].

## ما تم بناؤه

أضيفت خطة `ExcelWorkbookPlan` تحتوي على أوراق عمل وجداول وصفوف وأعمدة وخلايا typed. كل خلية تحمل النص الأصلي، القيمة الآمنة، النوع، اللغة، الثقة، الإحداثيات، معلومات الدمج المؤكد، وتنسيقًا محافظًا. الأنواع المدعومة هي النص والرقم والتاريخ والنسبة والعملة. الأرقام العربية تُحوّل إلى قيم رقمية فقط عندما لا توجد أصفار افتتاحية؛ أما أرقام الحسابات والفواتير والمنتجات ذات الأصفار الأولى فتبقى نصًا. لا تُخترع الصيغ.

تدعم الواجهة أوضاع PDF → Excel وصورة → Excel والتحويل الذكي، واختيار وضع توزيع الجداول على أوراق منفصلة أو ورقة واحدة أو تلقائي. التحويل الذكي يضع جدولًا واحدًا في ورقة `Nawa OCR`، ويقسم الجداول المتعددة إلى أوراق منفصلة. تعرض المعاينة القيم في خلايا وصفوف حقيقية قبل الإدراج، وتعرض metadata أولية تشمل الصفحات وعدد الجداول المتوقع واللغة المكتشفة.

تُنفّذ حدود النطاق، التفاف النص، الملاءمة التلقائية للأعمدة والصفوف، المحاذاة حسب RTL/LTR، والعناوين الغامقة. عند وجود دمج مؤكد فقط يُنشأ نطاق الدمج، وتُفرّغ الخلايا المغطاة في مصفوفة الإدراج لمنع تكرار قيمة المرساة. الدمج غير المؤكد لا يُنفذ.

## الملفات الرئيسية

| الملف | الدور |
|---|---|
| `shared/excel.ts` | نموذج خطة Excel، تصنيف الأنواع، الصفوف والأعمدة، الدمج، ومنطق smart |
| `shared/excel-metrics.ts` | قياسات بنيوية قابلة لإعادة الإنتاج على fixture |
| `server/excel.test.ts` | اختبارات الأنواع والقيم والصفوف والأعمدة والتنسيق والدمج |
| `server/routers.ts` | إجراء `excel.transform` والتحقق من توافق نوع العملية مع الملف |
| `client/src/pages/Home.tsx` | وضع Excel، المعاينة، خيارات الأوراق، وإدراج `Excel.run` |
| `research-excel-stage3.md` | مصادر Microsoft والقرار المعماري |

## الاختبارات والقياس

نتج عن التشغيل الأخير **25 اختبارًا ناجحًا من أصل 25** عبر أربع مجموعات اختبار، مع نجاح `pnpm check` و`pnpm build`. تغطي الاختبارات العربية، الفرنسية والإنجليزية المختلطة، الأرقام العربية، الأصفار الافتتاحية، النسب، التواريخ، العملات، مصفوفة القيم، الصفوف، الأعمدة، التنسيق، A1 addresses، الدمج المؤكد، وعدم تنفيذ الدمج غير المؤكد، كما يثبت اختبار payload runtime أن أوضاع PDF/Image/Smart المختارة في Task Pane تنتج request صحيحًا وتستدعي mutation، ويغطي اختبار إضافي التحويل الذكي لجدولين إلى ورقتين منفصلتين والقياسات البنيوية للـ fixture، كما تغطي اختبارات أخرى استنتاج صفوف وأعمدة جدول بلا حدود من polygon الخام ودمج جدولين متتابعين مع رأس مكرر عبر صفحتين. هذه قياسات fixture اصطناعي صريح وليست benchmark OCR خارجيًا.

على fixture الصريح المستخدم في الاختبار: Cell Accuracy = 1.000، Row Accuracy = 1.000، Column Accuracy = 1.000، Table Structure Accuracy = 1.000، Numeric Accuracy = 1.000، Date Accuracy = 1.000، Currency Accuracy = 1.000، وRTL Accuracy > 0.500. هذه ليست دقة OCR لمستندات المستخدم؛ لا يمكن تقديم قياسات حقيقية قبل مزود OCR فعلي وملفات مرجعية موسومة، ولذلك لا أضع نسبًا تخمينية. كما لم يتم ادعاء تنفيذ دورة Excel المكتبي: تعديل خلية، SUM/SORT/FILTER، الحفظ، الإغلاق وإعادة الفتح؛ ذلك يحتاج Excel فعليًا وملفًا حقيقيًا.

## المشاكل والحدود المتبقية

أضيف الآن مستنتج بنيوي من خلايا OCR الخام وإحداثيات polygon لتجميع الصفوف والأعمدة واكتشاف الجداول بلا حدود، مع دمج الصفحات المتتابعة عند تطابق الرؤوس. يظل تحويل صورة خام إلى OCR raw مسؤولية مزود OCR؛ لا يتم اختراع نص غير موجود. كذلك فإن الصور تعرض لغة `unknown` قبل OCR الحقيقي، ولا يوجد قياس دقة حقيقي دون Azure أو مزود OCR آخر. RTL ممثل في الخطة والمحاذاة، لكن اتجاه ورقة Excel الكامل يظل مقيدًا بما تدعمه نسخة Excel.js المستهدفة.

لم تبدأ المرحلة الرابعة: لا توجد مراجعة ذكية متقدمة، ولا مقارنة بصرية نهائية، ولا تحويل جماعي، ولا اشتراكات أو دفع.

## طريقة التشغيل

شغّل `pnpm check` ثم `pnpm test` ثم `pnpm build`. لعرض Excel الحقيقي يجب فتح الإضافة داخل Excel أو بيئة Office تدعم `Excel.run`. عند توفر مزود OCR صحيح، يُستبدل المسار التجريبي عبر factory دون تغيير نموذج خطة Excel أو واجهة المعاينة.

## المراجع

[1]: https://learn.microsoft.com/en-us/office/dev/add-ins/excel/excel-add-ins-ranges-get "Get Excel worksheet ranges using the JavaScript API — Microsoft Learn"

[2]: https://learn.microsoft.com/en-us/office/dev/add-ins/excel/excel-add-ins-tables "Create, read, and manage Excel tables with JavaScript — Microsoft Learn"
