# تقرير المرحلة الأولى — Nawa OCR Office

## الخلاصة التنفيذية

تم بناء أساس مشترك لإضافة Office تعتمد Office.js وReact وTypeScript، مع Task Pane باسم **Nawa OCR** يعمل بواجهة عربية RTL وقابل للتبديل إلى الفرنسية أو الإنجليزية LTR. يحتوي المشروع على خط رفع ومعالجة أولي، نموذج نتائج موحّد مستقل عن Word وExcel، محول OCR قابل لتبديل المزود، معاينة للنص والثقة والجداول، وكود إدراج أولي في Word وExcel. لم تبدأ المرحلة الثانية.

## ما تم بناؤه

| المجال | التنفيذ |
|---|---|
| بنية الإضافة | `manifest.xml` يعرّف مضيفي Document وWorkbook ويفتح Task Pane مشتركًا. |
| الواجهة | `client/src/pages/Home.tsx` يقدّم رفع الملف، اختيار لغة OCR، حالة المعالجة، المعاينة، ومفتاح لغة الواجهة. |
| Office.js | تحميل المكتبة من CDN الرسمي في `client/index.html` مع `Word.run` و`Excel.run` للإدراج التجريبي. |
| خط OCR | `server/ocr.ts` يحتوي واجهة `OCRProvider` ومحول Azure Document Intelligence Layout مع fallback محلي واضح عند غياب مفاتيح المزود. |
| النموذج الموحد | `shared/ocr.ts` يعرّف Document → Pages → Blocks → Lines → Words وTables → Rows → Columns → Cells، مع اللغة والإحداثيات والثقة. |
| الملفات | قبول PDF وPNG وJPG/JPEG وTIFF وWEBP، مع تصنيف PDF النصي/الممسوح/المختلط بالمسار الأولي. |
| preprocessing | `server/preprocess.ts` يستخدم sharp للتدوير التلقائي، قص الخلفية، تحسين التباين، الحدة، وإعادة التحجيم قبل OCR للصور. الأصل لا يُعدّل، وتُصفّر البيانات المؤقتة في الذاكرة بعد المعالجة. |
| الخصوصية | لا تُرسل مفاتيح المزود إلى الواجهة، ولا يُحفظ الأصل في قاعدة البيانات، وتُعلن النتيجة أن الأصل غير محتفظ به والبيانات المؤقتة محذوفة. |
| الأخطاء | توجد رسائل عربية لحالات النوع غير المدعوم، الملف الفارغ، الصورة غير المقروءة، مهلة OCR، وفشل الخدمة، مع رسالة عامة آمنة للحالات غير المعروفة. |

## قرار محرك OCR

تم اختيار **Azure Document Intelligence Layout** كمحول أول، وليس كارتباط دائم بالواجهة. السبب التقني هو أن وثائق Microsoft الرسمية تذكر أن نموذج Read يدعم النص المطبوع بالعربية والإنجليزية والفرنسية، وأن نموذج Layout يستخرج النص والجداول وبنية المستند. كما توضح الوثائق أن النماذج متعددة اللغات تستخرج الأسطر ذات اللغات المختلطة، وأن تحديد اللغة اختياري وقد يسبب نتيجة ناقصة إذا فُرضت لغة خاطئة [1]. لذلك يبقى اختيار المزود خلف `OCRProvider` ويمكن استبداله لاحقًا دون تغيير واجهة النتائج.

في بيئة التطوير التي لا تحتوي مفاتيح Azure، يستخدم المشروع fallback محليًا معلّمًا بوضوح باسم `demo-local` حتى يمكن اختبار بنية التدفق دون ادعاء أنه OCR إنتاجي. لا ينبغي اعتبار هذا fallback قياسًا لدقة التعرف العربية.

## الاختبارات والنتائج الرقمية

تم تشغيل الأوامر التالية بنجاح:

```text
pnpm check
pnpm test
pnpm build
```

النتيجة: **2 ملفات اختبار ناجحة، 7 اختبارات ناجحة، 0 فشل**. شملت الاختبارات قبول الصيغ المطلوبة، تصنيف PDF النصي والممسوح والمختلط، اكتشاف العربية والنص المختلط، وإرجاع نموذج موحد مع الثقة وضابط حذف البيانات المؤقتة. كما نجح بناء Vite وخادم Node للإنتاج. ظهر تحذير حجم حزمة JavaScript بعد التصغير، ولم يمنع البناء.

تم تنفيذ تحقق بصري للواجهة على سطح المكتب. الواجهة ظهرت RTL ببطاقات رفع ومعاينة واضحة، ثم أضيفت معالجة overflow لتناسب Task Pane. لم يتمكن الاختبار الحالي من فتح Word أو Excel الحقيقيين داخل جلسة Office، ولذلك لا أقدّم ادعاءً بأن sideload أو الإدراج الفعلي على سطح المكتب قد تم التحقق منه.

## الملفات الرئيسية

| الملف | الغرض |
|---|---|
| `manifest.xml` | تعريف المضيفين Word وExcel وTask Pane والأوامر. |
| `client/index.html` | تحميل Office.js الرسمي ونقطة دخول الإضافة. |
| `client/src/pages/Home.tsx` | تجربة Task Pane والمعاينة والإدراج. |
| `server/ocr.ts` | تصنيف الملفات، محرك Azure، fallback، الثقة، والأخطاء. |
| `server/preprocess.ts` | preprocessing فعلي للصور باستخدام sharp. |
| `shared/ocr.ts` | عقد البيانات الموحد وواجهات المزود ومحوّل المعاينة الثابت. |
| `server/ocr.test.ts` | اختبارات قبول الملفات، اللغة، التصنيف، المصنع، العقد، والخصوصية. |
| `server/ocr.test.ts` | اختبارات نواة OCR. |
| `research-ocr.md` | ملخص دراسة اختيار المزود. |
| `todo.md` | سجل التنفيذ والفجوات المتبقية. |

## المشاكل والحدود الحالية

أولًا، تشغيل OCR الفعلي بالعربية والجداول يتطلب إضافة `AZURE_DOCUMENT_INTELLIGENCE_ENDPOINT` و`AZURE_DOCUMENT_INTELLIGENCE_KEY` كأسرار بيئية في إعدادات المشروع. لم تُرسل هذه القيم في الطلب، لذلك لا يمكن احتساب benchmark حقيقي لدقة العربية أو WER/CER أو دقة الخلايا.

ثانيًا، كود `Word.run` و`Excel.run` وManifest موجودان، لكن التحقق التشغيلي داخل تطبيقات Office الحقيقية يحتاج sideload من Microsoft 365 أو Office Desktop مع عنوان HTTPS منشور وشهادة موثوقة. البيئة الحالية تحققت من TypeScript والبناء، لا من مضيف Office حقيقي.

ثالثًا، تصنيف PDF الحالي أولي قائم على مؤشرات بنية PDF، وليس محلل PDF كاملًا لاستخراج النص الأصلي صفحة بصفحة. يلزم في المرحلة التالية من خط OCR إضافة محلل PDF ناضج واختبارات بملفات PDF حقيقية ممسوحة ومختلطة.

## طريقة التشغيل

```text
cd /home/ubuntu/nawa-ocr-office
pnpm install
pnpm dev
```

لإضافة Office، استخدم `manifest.xml` عبر خيار **Upload My Add-in** أو sideload في Word وExcel بعد توفير عنوان HTTPS متاح للمضيف. في التطوير المحلي قد يلزم تشغيل HTTPS موثوق، وهو شرط من شروط Office Add-ins وليس جزءًا من منطق OCR نفسه.

## قرار الجاهزية

المرحلة **جاهزة معماريًا للانتقال إلى اختبار تكاملي مضبوط**، لكنها ليست جاهزة بعد لإعلان دقة OCR إنتاجية أو اعتماد Word/Excel النهائي. وفق طلب المستخدم، توقفت هنا ولم أبدأ المرحلة الثانية. الانتقال الرسمي ينبغي أن يسبقه تزويد مفاتيح Azure، تشغيل مجموعة ملفات عربية ومختلطة حقيقية، sideload في Word وExcel، ثم تسجيل قياسات الدقة والزمن ودقة الجداول.

## المراجع

[1]: https://learn.microsoft.com/en-us/azure/ai-services/document-intelligence/language-support/ocr?view=doc-intel-4.0.0 "Language and locale support for Read and Layout document analysis — Microsoft Learn"
