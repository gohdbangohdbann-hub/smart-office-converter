# تقرير المرحلة الرابعة — Intelligent Verification

## النطاق

تم تنفيذ مرحلة التحقق الذكي داخل نطاق **local-only**. لا تعتمد المرحلة على مفاتيح Azure، ولا تدّعي قياس دقة OCR واقعية أو نجاح sideload داخل Word/Excel المكتبي.

## ما تم تنفيذه

أضيف العقد المشترك `VerificationIssue` و`VerificationReport` و`ReviewAction` في `shared/verification.ts`، مع ثبات القيمة الأصلية وعدم إجراء أي تعديل تلقائي (`automaticChanges: 0`). يحسب المحرك درجة ثقة مشتقة من ثقة كتل OCR والجداول، ويصنف الإشارات إلى موثوق، يحتاج مراجعة، وعالي الخطورة.

يكشف المحرك انخفاض الثقة، مؤشرات تشوه النص العربي والمسافات وعلامات الترقيم، النص المختلط RTL/LTR، التباس الحروف والأرقام العربية/الغربية، إشارات بنية الجدول والخلايا الفارغة والتكرار، والتعارض الحسابي المحافظ عندما تكون أعمدة الكمية والسعر والمجموع واضحة. الاقتراحات غير ملزمة وتحافظ على النص المستخرج حتى يراجع المستخدم النتيجة.

أضيف `VerificationPanel` إلى `Home.tsx`. يعرض المركز ملخصًا للثقة وعدد الكلمات والخلايا والقضايا، وSplit View بين الملف المصدر والناتج المستخرج، وقائمة قابلة للتنقل، ومحررًا لقضية واحدة، وإجراءات قبول وتعديل وقبول كل الموثوق. كما يسجل Before/After/Method في سجل محلي داخل الجلسة. زر إعادة OCR المحلية يطبق التطبيع المقترح فقط عند توفره، ولا يعيد معالجة المستند كاملًا ولا يرفع الملف إلى خدمة إضافية.

## التحقق المحلي

| الفحص | النتيجة |
|---|---:|
| TypeScript strict check | ناجح |
| Vitest files | 6 |
| Vitest tests | 31 ناجحًا |
| Production build | ناجح |
| اختبارات الأخطاء المتعمدة | ناجحة |

تغطي اختبارات `server/verification.test.ts` انخفاض الثقة، التباس الرقم والحرف، النص العربي المختلط، التعارض الحسابي، الحفاظ على القيمة الأصلية، وتسجيل إجراء مراجعة المستخدم. تحذير البناء الوحيد المتبقي متعلق بحجم حزمة الواجهة، وليس خطأ تشغيل.

## الحدود الصريحة

لم تُستخدم بيانات تقييم مصطنعة لقياس نسبة دقة. التحقق من جودة OCR الفعلية يحتاج ملفات benchmark عربية مرجعية ومزود OCR حقيقي، كما أن إدراج النتيجة داخل تطبيقات Word وExcel المكتبية يحتاج sideload ودورة تشغيل خارج المعاينة المحلية. تبقى هذه العناصر خارج نطاق هذه المرحلة.

**لا تبدأ المرحلة الخامسة قبل مراجعة هذا التقرير.**
